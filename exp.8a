import pandas as pdimport numpy as npfrom sklearn.ensemble import AdaBoostClassifierfrom sklearn.tree import DecisionTreeClassifierfrom mlxtend.plotting import plot_decision_regionsimport matplotlib.pyplot as plt# Step 1: Create datasetdf = pd.DataFrame()df['X1'] = [1,2,3,4,5,6,6,7,9,9]df['X2'] = [5,3,6,8,1,9,5,8,9,2]df['label'] = [1,1,0,1,0,1,0,1,0,0]# Step 2: Prepare features and labelsX = df[['X1', 'X2']].valuesy = df['label'].values# Step 3: Initialize base (weak) learnerbase_clf = DecisionTreeClassifier(max_depth=1)# Step 4: Create AdaBoost classifierada = AdaBoostClassifier(estimator=base_clf, n_estimators=10, learning_rate=1.0, random_state=1)# Step 5: Train AdaBoostada.fit(X, y)# Step 6: Plot decision regionsplt.figure(figsize=(8,6))plot_decision_regions(X, y, clf=ada, legend=2)plt.title('AdaBoost Decision Boundary')plt.xlabel('X1')plt.ylabel('X2')plt.show()
