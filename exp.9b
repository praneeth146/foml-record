librariesimport numpy as npimport matplotlib.pyplot as pltfrom sklearn.cluster import KMeans# Step 1: Create sample data (2D points)np.random.seed(42)X = np.random.rand(100, 2) # 100 points with 2 features# Step 2: Choose number of clusters (k)k = 3# Step 3: Create and train the K-Means modelkmeans = KMeans(n_clusters=k, init='k-means++', random_state=42)kmeans.fit(X)# Step 4: Get cluster predictions and centroidslabels = kmeans.predict(X)centroids = kmeans.cluster_centers_# Step 5: Visualize clustersplt.figure(figsize=(8,6))plt.scatter(X[:,0], X[:,1], c=labels, cmap='rainbow', s=50)plt.scatter(centroids[:,0], centroids[:,1], c='black', marker='X',
s=200, label='Centroids')plt.title('K-Means Clustering (k=3)')plt.xlabel('Feature 1')plt.ylabel('Feature 2')plt.legend()plt.show()
#
