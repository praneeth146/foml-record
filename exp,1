import pandas as pdimport matplotlib.pyplot as pltimport seaborn as snsimport numpy as np# Load datasettry: df = sns.load_dataset('iris') print(df.shape) print(df.head())  species = df['species'].unique() # Function to plot univariate scatter def univariate_plot(feature): plt.figure(figsize=(6, 3)) for sp in species: subset = df[df['species'] == sp] plt.scatter(subset[feature], np.zeros_like(subset[feature]), label=sp) plt.xlabel(feature) plt.legend() plt.title(f'Univariate Analysis of {feature}') plt.show() # Plot each feature for feature in ['sepal_width', 'sepal_length', 'petal_width', 'petal_length']: univariate_plot(feature) sepal.width vs petal.width sns.FacetGrid(df, hue='species', height=5).map(plt.scatter, "sepal_width", "petal_width").add_legend() plt.title('Bivariate: Sepal Width vs Petal Width') plt.show() # sepal.length vs petal.length sns.FacetGrid(df, hue='species', height=5).map(plt.scatter, "sepal_length", "petal_length").add_legend() plt.title('Bivariate: Sepal Length vs Petal Length') plt.show()
sns.pairplot(df, hue="species", height=2) plt.suptitle('Multivariate Pairplot of Iris Features', y=1.02) plt.show()except FileNotFoundError: print("Error: The file 'iris.csv' was not found. Please check the file path.")
(150, 5) sepal_length sepal_width petal_length petal
